


import 'package:flutter/material.dart';

class Dashboard extends StatelessWidget {
  BuildContext? context;
   Dashboard({this.context , super.key});

  @override
  Widget build( context) {
    return  Container(color: Colors.blue,);
  }
}